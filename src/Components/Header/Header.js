import React from 'react';

import './Header.css'

function Header() {
    return (
        <header className="App-header">
            <h1 className="header-title">Список задач</h1>
        </header>
    );
}

export default Header;